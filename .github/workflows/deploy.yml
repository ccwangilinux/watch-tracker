name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# 使用 OIDC 部署，不需要任何 secret
permissions:
  contents: read
  pages: write
  id-token: write

# 同時只跑一個部署，但不取消進行中的，避免半途中斷留下壞掉的線上版本
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # 測試不過就不要部署上去
      - run: npm test

      - run: npm run build

      - uses: actions/configure-pages@v5
        with:
          # 第一次執行時自動啟用 repo 的 Pages 設定，
          # 省去手動到 Settings → Pages 選 Source 的步驟
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
