# Google OAuth 用戶端 ID。
#
# 這個值設計上就是公開的（會出現在前端程式碼中），可以安全地寫在這裡。
# 真正不能外流的是 Client Secret —— 本專案完全不需要它，也絕不可加入。
#
# 取得方式：Google Cloud Console → API 和服務 → 憑證 → 建立 OAuth 用戶端 ID
#   類型：網頁應用程式
#   已授權的 JavaScript 來源：
#     http://localhost:5173
#     https://ccwangilinux.github.io
#   （token flow 走 popup，不需要填 redirect URI）
VITE_GOOGLE_CLIENT_ID=
